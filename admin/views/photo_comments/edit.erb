<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:photo_comments, :index) %></li>
  <li> <%= link_to tag_icon(:plus, pat(:new)), url(:photo_comments, :new) %></li>
  <li class="active"> <%= link_to tag_icon(:edit, pat(:edit)), url(:photo_comments, :edit, :id => @photo_comment.id) %></li>
</ul>
<div class="tabs-content">
  <% form_for :photo_comment, url(:photo_comments, :update, :id => @photo_comment.id), :method => :put, :class => 'form-horizontal' do |f| %>
    <%= partial 'photo_comments/form', :locals => { :f => f } %>
  <% end %>
</div>
